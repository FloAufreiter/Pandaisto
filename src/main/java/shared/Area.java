package shared;
//TODO setup Graph of Area

import java.util.Random;

public class Area {

    //Dummy value for distance TODO remove
    static Random random = new Random();

    static int computeDistance(Location location1, Location location2) {
        return random.nextInt(50);
    }
}
